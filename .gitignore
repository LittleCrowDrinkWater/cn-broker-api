# ── 绝不入库：凭据与运行期状态 ────────────────────────────
# 本仓库计划公开。凡是「这台机器特有」或「能登进别人账户」的东西一律挡在这里。
# ⚠️ 加规则要往这一段加，别往下面的常规段加——常规段是给工具垃圾用的，
#    混在一起以后没人分得清哪条是安全规则、哪条能随便删。
*.toml
!config.example.toml
token.txt
*token*
*secret*
*credential*
*password*
*.pem
*.key
tdx-login.json
# ⚠️ 只挡仓库根下的运行期状态目录。**不能写成 `state/`**：那条会连源码包
#    `cn_broker_api/state/` 一起挡掉，而 git 不报错、`git add -A` 也不报错
#    ⇒ 推上去的仓库 clone 下来直接 import 失败（2026-08-22 真的发生过一次）。
/state/
*-state/
logs/
*.log
# 窗口截图会把账号、持仓、资产原样拍进去
*.png
*.jpg
*.bmp
diag/

# ── Python ──────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# ── 编辑器与系统 ─────────────────────────────────────────
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
