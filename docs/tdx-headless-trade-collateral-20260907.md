# HQMP 担保买入样本归档（2026-09-07）

## 样本范围

本次在实验副本中由用户登录融资融券账户后，手动提交一笔明确标注为“担保买入”的限价委托，随后撤单。原始 JSONL 抓包保留在实验目录外：

`D:\Common\TDXV2026X64_trade_lab_20260903\trade_lab\hqmp-proxy-20260907.jsonl`

本页只保存脱敏后的结构和统计信息，不复制原始 Base64、账户标识或委托编号。

## 委托结果

- 标的：`000001.SZ`
- 方向：买入
- 类型：担保买入
- 价格：`10.720`
- 数量：`300`
- 柜台状态：先进入委托列表，随后撤单
- 最终状态：`Status=0`
- 成交量：`CjVol=0`

因此本次没有成交，可作为明确的“担保买入、已撤、零成交”样本。

## 抓包观察

提交窗口中观察到以下协议流量：

1. 一帧 `client_to_tc` 的 `179` 字节 `rpcfunction`；
2. 随后出现 `tc_to_client` 的 `226` 字节 `callfunction`；
3. 紧接着出现 `4300`、`4292`、`2420` 和 `684` 字节的较大 `rpcfunction`；
4. 同一窗口还出现 `170`、`234`、`218`、`202`、`210` 和 `226` 字节的 `callfunction`，并伴随 `resultType=nullptr` 回执。

撤单窗口再次出现相同类别的大型 RPC 流量。`210`、`226` 等短帧在提交和撤单阶段都会重复，不能单独视为本次订单的业务参数帧。

## 当前判断

- HQMP 的 20 字节外壳、帧边界和 `callfunction`/`rpcfunction` 标记仍可稳定解析。
- 本次明确的担保买入样本没有在解码后的帧中出现 `000001`、`10.72`、`300` 或委托编号等明文业务字段。
- 用户已确认当天 200 股和 300 股两笔均为担保买入；类型不确定的是最早的历史样本。两笔仍需核对实际调用入口，不能只按帧长度认定为同一种报单请求。
- 业务正文尚未解码，不能确认每个候选帧是否承载订单参数，也不能据此确定压缩或加密算法。

## 后续边界

后续分析只针对实验目录外的原始 JSONL 做离线差分，优先比较本样本中的 `179/4300/4292/2420/684` 字节 `rpcfunction`。在定位稳定字段之前，不重放原始报文，也不据此自动生成真实委托。

## 后续离线解码结果（更正上述未解码阶段判断）

2026-09-07 继续静态分析后，已使用本地 DLL 中的初始化材料离线还原三份抓包的业务 JSON，未启动客户端或发送交易请求。

- `tdxrpc64.dll` 的 SHA-256 为 `f0855f368084235afa2a5d1d19cd1cce326117b623fe503fb26fc2146fc97a0c`。
- 该版本 RVA `0x14f0` 初始化 Blowfish 的 P/S 表，`0x15c0` 扩展密钥，`0x1860` 按 8 字节块加密，`0x18f0` 解密。编码使用 ECB，每个 32 位字以小端布局存储；补齐 n 字节时使用 n 个 ASCII 数字 n，而非标准 PKCS#7 字节值。
- 密钥初始化由 RVA `0x1100` 引用本地常量；工具仅对上述哈希使用已核验 RVA，不在代码或报告中保存密钥。
- 成功历史抓包解码 160 个正文，拒绝历史抓包 74 个，当日抓包 309 个，合计 543 个，全部通过填充及 JSON 校验。统计针对本次读取到的快照。
- 旧 411 字节帧实际为 `DoLevinGN_909`，参数包含 `zqdm=000001`、`price=10.720000`、`wtsl=100`、`bsflag=0`、`flag=0`。这些数字标志的融资/担保业务语义仍需独立核验。
- 226 字节帧为 `DoLevinGN_30`，含 `ksrq`、`zzrq`、`zqdm` 等查询参数。两处差异是起止日期从 `20260907` 变为 `20260904`，不是委托数量变化。
- 4300、4292、2420、684 字节候选帧实际为 `ReturnValueComp`；分别关联 `DoLevinGN_807`、`DoLevinGN_822`、`DoLevinGN_803`、`DoLevinGN_807` 的返回数据，不应当作下单入口。
- 当日两笔担保买入所在抓包未出现 `DoLevinGN_909`，不能再把这些时间窗口称为已捕获完整报单链路。仍需调查手动担保买入是否使用 TC 内部的另一入口。

可复查工具：`cn_broker_api.drivers.tdxquant.hqmp_capture`。开发虚拟环境需安装 `pefile` 和 `pycryptodome`；仅分析反汇编时使用 `capstone`，这些依赖不参与交易服务运行。工具按连接和方向重新组帧，默认只输出方法统计、参数名、失败行号，不输出业务值：

```powershell
.venv\Scripts\python.exe -m cn_broker_api.drivers.tdxquant.hqmp_capture --dll D:\Common\TDXV2026X64_trade_lab_20260903\tdxrpc64.dll D:\Common\TDXV2026X64_trade_lab_20260903\trade_lab\hqmp-proxy-20260907.jsonl
```

代理两个方向共享 `FrameBuffer` 的问题已修正，并增加双向半帧交错测试。既有三份抓包按原始双向字节流重新拼接，与已记录帧一致，未观察到该缺陷影响这些样本。

## 方法与回调配对

对三份快照按 `(method, token)` 与回调中的 `(functionname, token)` 配对，分别得到 72、31、129 组完整配对，共 232 组；没有未匹配回调或待返回调用。`type=0` 的 `nullptr` 是外层 RPC 返回，业务结果应读取另一个方向的 `ReturnValueComp.params.value`。

| 方法 | 已观察到的结果字段及用途 |
|---|---|
| `OperateUser_0` | 登录账户列表，含账户、券商和登录类型信息 |
| `DoLevinGN_803` | 证券数量、可卖数量、成本和市值，属于持仓数据 |
| `DoLevinGN_830` | 可用资金、余额、资产及负债，属于资金数据 |
| `DoLevinGN_807` / `DoLevinGN_822` | 委托数量、价格、成交量和撤单状态；两者的筛选差异尚未确定 |
| `DoLevinGN_909` | 报单回执：`retflag`、`retinfo`、`wtbh` |
| `DoLevinGN_808` | 撤单回执：`success`、`errmsg` |

旧 100 股样本在 JSONL 第 1446 行调用 `DoLevinGN_909`，第 1447 行通过 `ReturnValueComp` 返回 `retflag=1`、非空委托编号、空 `retinfo`。查询结果随后出现对应委托，并最终变为已撤。

当日 200 股、300 股均可从解密后的查询回调观察到：价格 `10.7200`、`bsflag=0`、`cjsl=0`；状态从 `cdflag=0 / kcdflag=1 / 买入@正常委托@` 变为 `cdflag=1 / kcdflag=0 / 买入@已撤@`。这些是本样本的取值，不应扩展为所有状态的通用枚举。

实验副本 `PYPlugins/user/tqcenter.py` 的常量表定义 `CREDIT_BUY=0`、`CREDIT_FIN_BUY=69`，但这是上层接口枚举，尚不能独立证明 HQMP 的 `bsflag/flag` 与之完全等价。旧 100 股单的具体融资/担保类型继续保留为待核验。

后续优先检查 `DoLevinGN_909` 的调用入口及账户映射，先实现直接 TC 的只读会话和回调处理。现有手动操作的结果可查，但缺少对应 HQMP 报单调用；不能把查询结果误作已掌握担保买入请求编码的证据。

离线工具的可选依赖固定在仓库根目录 `requirements-hqmp-analysis.txt`。默认输出仅包含方法、字段名和配对统计，原始密钥、账户信息及解密业务值不提交仓库。

## 直接 HQMP 会话进度（2026-09-08）

已把实验代码收进 `cn_broker_api.drivers.tdxquant.hqmp_direct`：

- 使用已核验版本的 `tdxRpc64.dll` 启动 RPC 服务，只接受带 `.trade-lab-marker` 的实验副本；
- 能接收 TC 的 `RegisterClient` 以及 `NotifyMsgClient`，并区分注册与登录就绪；
- 已按抓包实现账户、持仓、资产、委托查询的回调关联；
- 已按唯一成功样本封装 `DoLevinGN_909` 报单字段，并按厂商 `tqcenter.py` 读取委托类型编号；
- 已封装 `DoLevinGN_808` 撤单字段；交易调用必须在创建会话时显式打开交易闸；
- 报单或撤单发出后等待回调超时会归为状态未知，不能当作未发送后自动重试。

当前真机边界：TC 可以注册并发出登录就绪通知，`callRpcClientInterfaceByToken` 对
`DoLevinGN_927`、`OperateUser_0` 均返回已入队，但 TC 没有返回 `ReturnValueComp`，所以直接
会话的服务端发送调用仍未闭环。报单与撤单封装目前只通过离线样本和单元测试核对，**尚未由这份
仓库代码发送真实委托**。

下一步应找回或重建 2026-09-07 晚间产生 `hqmp-direct-13575.out.log` 的成功直连发送实现，
对比其调用入口与当前 ctypes 调用；在只读回调闭环前不进入真实报单验收。

### 后续恢复结果（2026-09-08）

已从 2026-09-07 的本地开发会话记录找回当晚未提交的成功实现，确认它使用纯 Python 构造
HQMP 帧，而不是 `callRpcClientInterfaceByToken`。这解释了 ctypes 原型只能接收 TC 注册、
服务端调用显示入队却没有业务回调的现象。

当前代码已经改回已验证的纯协议路径：

- 从外部抓包提取对应方法的帧头模板，不读取或记录模板中的业务值；
- 从已核验哈希的本地 DLL 提取密钥，按 GB18030、Blowfish ECB 和已观察到的补齐规则编码；
- 使用本次 TCP 连接给出的实时 `callfunction` GUID，不复用历史连接 GUID；
- 复现 `927 -> OperateUser_0 -> 809/807/822/920/803/830` 初始化查询顺序；
- 资金账号、券商编号和用户标识从 `OperateUser_0` 动态建立，不再硬编码实验账户；
- 新增 `--reuse-tc`，只在进程镜像路径精确属于实验副本时接管已登录 TC；
- 冷启动等待期间若 TC 提前退出，会立即报告退出码，不再空等到总超时。

相关离线测试已经通过。2026-09-08 的真机复验中，实验 TC 先连接原厂 HQMP 宿主并由操作者
完成登录，随后断开原厂宿主、保留 TC，并由纯 Python 宿主接管重连。复用的已登录会话不会
再次发送一次性的登录就绪通知，因此 `--reuse-tc` 只等待 `RegisterClient`，冷启动仍同时等待
登录就绪通知。

纯 Python 宿主随后完成了全部只读初始化调用并收到 `ReturnValueComp`：返回 1 个账户、5 条
持仓、1 条资产记录和 0 条当日委托。输出只保留条数，没有记录账户标识和业务明细。此次没有
调用 `DoLevinGN_909` 或 `DoLevinGN_808`，因此直接 HQMP 的账户、持仓、资产和委托查询链路
已真机闭环，下一步才是使用明确的小额参数验收真实报单及撤单。

### 真实报单与撤单验收（2026-09-08）

操作者明确授权后，纯 Python HQMP 宿主在实验 TC 的已登录融资融券账户中完成了一次真实闭环：

- 标的为平安银行 `000001.SZ`，类型为担保买入，限价 `10.61` 元，数量 `100` 股；
- 报单 `DoLevinGN_909` 返回成功标志和非空委托编号；仓库和本文均不保存完整编号；
- 紧接报单回调发送的第一次 `DoLevinGN_808` 被柜台以“没有对应的委托信息”拒绝；
- 未重复报单，而是用 `DoLevinGN_807` 查询到该笔委托已进入“正常委托”、可撤且成交量为 0；
- 此后再次调用 `DoLevinGN_808`，回调为“撤单已报”；
- 最终通过 `DoLevinGN_822` 确认为“买入、已撤”，`cdflag=1`、`kcdflag=0`、成交量为 0。

这次结果证明 `DoLevinGN_909` 和 `DoLevinGN_808` 的构造、回调关联及账户动态映射均可真机工作。
同时确认了一个柜台可见性窗口：报单回调成功不代表撤单入口已经能查到该笔委托。自动化流程必须先按
委托编号查询到唯一、可撤的记录，再发送撤单；第一次“没有对应的委托信息”不能触发重复报单。

## 当前状态与后续待办（2026-09-10）

### 当前状态

- 开发分支为 `feature/tdx-headless-trade`；远端保留了上一轮快照，本轮直接交易接入与验收结果尚待提交；
- 纯 Python HQMP 已完成帧编码、连接 GUID 替换、请求/回调关联和账户动态映射；
- 账户、持仓、资产、委托四类只读查询已经真机闭环；
- 深市担保买入和撤单已经真机闭环，验收单成交量为 0，最终状态为已撤；
- 正式 HTTP/`QuantTradeDemo` CLI 已在非交易时段分别完成担保买入、融资买入及立即撤单，两笔均由柜台
  返回受理并最终确认已撤；因此不能再假设非交易时段一定拒单；
- 会话只接受带 `.trade-lab-marker` 的实验副本、已核验哈希的 DLL 和路径完全匹配的实验 `TC.exe`；
- 交易方法还要求显式打开 `enable_trade`，回调超时按状态未知处理，不自动重复报单；
- 直接 HQMP 已由正式 `TdxQuantDriver`、`TradingPort` 和 HTTP 端点装配，且已通过纯 headless 只读真机验收；
- 已加入明确的会话状态、只读状态接口和未登录错误契约；直接 HQMP 自动登录已接入正式驱动和登录接口；
- 冷启动已不再依赖原厂 HQMP 宿主热切换，但运行时仍依赖仓库外的历史抓包模板；断线长时运行和自动恢复尚未验收。
- 2026-09-10 冷启动曾出现界面已经登录、TC 到 13575 的 TCP 连接已建立，但服务未收到可识别
  `RegisterClient` 的竞态；保留已登录 TC 并重启 HQMP 宿主后立即恢复 `READY`。该恢复步骤尚未自动化。

2026-09-09 重新运行完整测试时曾发现自动确认脚本的跨午夜缺口：午夜后的陈旧信号落在前一天，
旧实现只计算 `TIME` 的当天秒数差，因结果为负数而将其当成未来信号跳过。现已使用 `DAY + TIME`
构造完整时间戳，并用固定时钟覆盖同日陈旧、跨午夜陈旧和未来时间三种情况；该修复完成后的完整测试已恢复全绿。

### 本轮实现进度

- 契约版本升级为 v5，新增 `READY`、`LOGIN_REQUIRED`、`LOGIN_IN_PROGRESS`、
  `MANUAL_ACTION_REQUIRED`、`LOGIN_LOCKED` 和 `CHANNEL_UNAVAILABLE` 会话状态；
- 新增只读 `GET /v1/session/status`。状态查询与账户业务请求都进入同一账户串行队列，避免并发调用交易通道；
- 账户和交易入口在发送业务调用前检查会话状态。明确未登录时返回 HTTP `503`、
  `broker_login_required`、当前状态和 `/v1/session/ensure`，不再混成空数据或未知 `500`；
- 登录状态机支持注入直接 HQMP 探针，并修复登录框持续存在时可能重复提交密码的问题：一次登录流程最多提交一次；
- `HqmpDirectSession.channel_ok()` 已可用 `RegisterClient`、账户查询和资产字段校验判定直接通道是否就绪；
- 独立直接 HQMP 命令新增实验性 `--auto-login`。它复用外部凭证文件、持久化提交闸门和已有窗口识别逻辑；
  如果通道本来已就绪则不会读取凭证，登录结束后只执行只读探测，不会自动报单；
- 直接 `DoLevinGN_909/808` 路径不经过页面待确认队列，因此不需要自动确认补丁；该补丁仅服务旧的
  `order_stock` 页面确认通道；
- `QuantTradeDemo` 调用方已在独立 `feature/broker-session-state` 分支升级到契约 v5，登录 CLI 先调用
  `/v1/session/status`，只有显式 `--go` 才从数据库解密交易密码并调用 `/v1/session/ensure`，成功后再次
  确认状态为 `READY`；定时任务和 CLI 已复用同一份凭据读取函数；
- 2026-09-09 已通过上述 CLI 完成一次正式 MCP/桌面通道的真实自动登录：初始明确返回
  `LOGIN_REQUIRED`（`TC.exe` 未启动），随后一次密码提交成功，账户与资产探针通过。此次没有发送交易指令；
- 已修复 `QuantTradeDemo` 的 `tdx_channel_check.py --at` 参数未传给服务的问题。旧行为会接受参数但不检查
  自动确认时段覆盖，现已把所有 `HH:MM` 转为 `need_times` 发给健康接口，并拒绝非法时刻；
- 直接 HQMP 新增可靠撤单流程：同时使用 `DoLevinGN_807/822` 按委托编号观察，等记录唯一可见且可撤后才
  发送 `DoLevinGN_808`，随后轮询至已撤、已成或状态未定。撤单回调超时只对账不重发；首次明确“没有对应
  委托”时，只有重新观察到同一编号仍可撤才允许第二次撤单；部分成交后撤销会保留已成交数量。
- 撤单重试授权已独立建模：第一次返回“没有对应委托”后，如果后续查询仍不可见，不会仅因进入下一轮循环
  就提前发送第二次撤单；只有新查询明确看到同一编号仍可撤时才重新授权一次发送。
- 新增 `HqmpDirectTrading` 适配层，已将直接会话的报单、可靠撤单、委托、持仓、可卖量和资产
  映射成统一交易契约；运输异常、柜台拒绝和回调超时保持不同错误类别。
- 适配层在任何交易和账户查询前核对 TC 当前选中的资金账号；不一致时在发单前拒绝，错误信息
  不回显请求或实际账号。撤单还会核对委托行的实际证券代码，防止错单。
- 委托身份只由规范化证券代码和市场确定。`DoLevinGN_909` 中的 `zqmc` 降为通道内部的
  可选展示字段，名称不可得时传空；不再要求 HTTP 调用方、自动策略或手工 CLI 传名称。
- 正式驱动现已支持显式 `transport = "hqmp"`，且只允许与 `desktop_mode = "headless"`、实验标记、
  已核验 DLL 和明确的抓包模板组合。启动时先监听 HQMP，登录接口之后只拉起 `TC.exe`；禁止静默回落。
- HTTP `TradingPort` 已移除 `security_name`；额外传入的名称不能覆盖代码身份。`QuantTradeDemo`
  的网关和 `tdx_order.py` 也只发 `symbol`，不再依赖 `cn_symbol_info` 完整性。
- 直接通道默认只读；打开交易闸后仍在发帧前执行独立的单笔数量和金额上限。

### 纯 headless 正式 HTTP 验收（2026-09-09/10）

- 先精确停止日常目录的 `Tdxw.exe` 和 `TC.exe`，确认 17710/13575 未被占用；
- broker API 以 `transport = "hqmp"` 启动后，17710 和 13575 均由 Python 服务持有，此时查不到任何
  `Tdxw.exe` 或 `TC.exe`；
- `QuantTradeDemo` 的 `tdx_login.py` 首先读到 `LOGIN_REQUIRED`，加 `--go` 后仅启动路径属于实验副本的
  `NewTc/TC.exe`，未出现 `Tdxw.exe` 或行情主页；
- 一次密码提交后状态转为 `READY`，HQMP 账户和资产探针通过；
- `tdx_channel_check.py` 确认 HQMP 宿主、账户、交易闸全绿，并明确说明直接通道无需页面自动确认；
- `tdx_order.py inspect` 经正式 HTTP 路由成功读到资产、5 条持仓和 0 条当日委托；
- 2026-09-10 00:20 后，经操作者明确授权，报单 CLI 以平安银行 100 股、10.61 元分别发送担保买入和
  融资买入；柜台在非交易时段仍受理两笔委托，可靠撤单随后均确认已撤；
- 对账读取到 5 条持仓和 2 条当日委托，通道、账户、交易闸与无需页面确认四项检查全部通过；
- 首次登录接口等待 240 秒后超时，但操作者界面已显示登录；此时 TC 到 13575 的 TCP 已建立，服务却尚未
  识别到 `RegisterClient`。只重启 Python HQMP 宿主并复用已登录 TC 后状态转为 `READY`，未再次提交密码；
- 直接会话已增加登录期间的单次注册恢复：TCP 连接持续 5 秒仍未注册时，只断开该 HQMP 连接
  促使 TC 重连，不重启 TC、不再次提交密码；断线后同时清除旧 token、就绪标志和账户缓存；
- 2026-09-10 16:28 收盘后，在禁用会自动重启的生产计划任务后重做全新冷启动。CLI 约 10 秒内
  从 `LOGIN_REQUIRED` 进入 `READY`，只有实验副本 `TC.exe`、无 `Tdxw.exe`；通道四项全绿，只读对账为
  5 条持仓、14 条当日委托。此次未复现卡死竞态；
- 2026-09-10 17:00 按操作者授权准备再次用仅证券代码的契约发送平安银行担保买入 100 股并立即撤单。
  行情库确认当日收盘 11.85 元、最低 11.66 元、昨收 11.70 元，预定限价为 10.54 元；登录接口只提交
  一次密码后，实验 `TC.exe` 已连接 13575，但 90 秒内没有产生可识别的 `RegisterClient`。通道检查保持
  失败关闭，因此报单 CLI 未执行，账户没有新增委托；
- 上述现场说明“仅断开未注册 TCP 促使 TC 重连”的恢复方式尚不能宣称有效。旧日志没有记录本轮是否实际
  执行过连接重置；现已把重置动作写入服务日志和最终登录错误详情，并修正超时文案，使其明确表达
  “密码已提交一次但通道未就绪”，而不是错误地声称没有见到登录框。失败后仍不重试密码；
- 2026-09-10 17:10 按操作者指示再次调用登录接口，本次约 13 秒进入 `READY`，账户和资产探针以及
  `tdx_channel_check.py` 四项检查全部通过。随后仅传 `000001.SZ`、100 股和 10.54 元担保买入参数，
  报单请求已经到达柜台，但柜台明确返回 `(410)不支持隔日委托`；没有形成有效委托，因此没有可执行的
  撤单。只读对账仍为 5 条持仓、14 条当日委托，确认没有新增委托；
- `RegisterClient` 只表示 TC 向 HQMP 宿主注册，不能单独代表交易账户已登录。当前 `READY` 判据还要求
  `OperateUser_0` 返回可选账户且 `DoLevinGN_830` 返回资金字段；TCP 已连接但没有 `RegisterClient`、
  同时没有明确登录框时应归类为 `CHANNEL_UNAVAILABLE`，不能猜成 `LOGIN_REQUIRED` 或 `READY`；
- 状态接口不依赖额外回调：当 TC 已连接、登录框已消失但 `RegisterClient` 可能迟到时，最多轮询 10 秒，
  每 0.5 秒检查一次；注册出现后立即重新执行账户与资产探针。TC 未启动、登录框仍存在或连接已断开时
  不等待，轮询超时仍返回 `CHANNEL_UNAVAILABLE`，且整个过程不提交密码、不发送或重试交易指令；
- 2026-09-10 21:26 真机复现了延迟注册异常：登录提交后第一条 HQMP 连接出现 `JSONDecodeError`，旧行为会
  让整个监听线程退出。现已改成单条连接异常只清理该连接并继续 `accept()`，帧边界完整但业务正文无法解码时
  只跳过该帧继续等待 `RegisterClient`。本轮随后仍未注册，登录接口按事实超时；状态接口的 10 秒重试实际
  耗时 10.43 秒并返回 `CHANNEL_UNAVAILABLE/已等待 10 秒仍未注册`，证明等待分支已在真机命中；
- 保留该 TC、只重启修正后的 HQMP 宿主并开启受控复用后，没有再次提交密码，状态接口 0.33 秒返回
  `READY`，`QuantTradeDemo` 通道检查四项全部通过。该恢复路径已再次真机验证，但尚未自动编排进登录接口；
- 2026-09-10 17:18 未提交密码、未发送交易指令，分别验证两种明确未登录状态。实验 TC 未启动时，
  `GET /v1/session/status` 返回 HTTP 200、`LOGIN_REQUIRED/ready=false`；`GET /v1/account` 返回 HTTP 503、
  `broker_login_required`、`/v1/session/ensure` 和 `retryable_after_login=true`。随后只启动实验 TC 并保留
  交易登录框，状态接口仍返回 `LOGIN_REQUIRED`，账户接口仍返回同一机器可读错误；此时 TC 尚未建立到
  13575 的 TCP 连接。这证明明确未登录由“TC 未启动或已识别登录框”判定，而不是由 `RegisterClient`
  缺失反推；
- 2026-09-10 21:47 使用 `QuantTradeDemo` 的 `tdx_login.py --go --type CREDIT` 再做一次全新冷启动，
  全程没有人工点击或切换宿主，约 12 秒从明确的 `LOGIN_REQUIRED` 进入 `READY`。进程中只有实验副本
  `TC.exe`，没有启动 `Tdxw.exe` 行情页面；四项通道检查全绿，资产、5 条持仓和 0 条当日委托的只读
  查询通过。会话就绪后再次调用 `--go` 幂等返回，密码提交计数保持 9 不变，没有重复填密码或点击登录；
  为越过已有连续失败闩锁，本轮只临时把本地忽略配置的提交上限调高，结束后已恢复为 10/3，历史计数未删除；
- 验收结束后已停止 broker API 和实验 `TC.exe`，17710/13575 均已释放。

正式 MCP/桌面通道的自动登录已经使用 `QuantTradeDemo` 数据库中的加密凭证完成一次真机验收；密码仅在
`--go` 后解密并随请求进入服务内存，没有写入日志或仓库。直接 HQMP 正式驱动的冷启动自动登录已使用
`QuantTradeDemo` 数据库加密凭证验收，未读取旧的仓库外明文凭证文件。

### 代码、验证与远端快照

- 服务端开发分支已于 2026-09-10 快进合入 `main` 并推送至 `7a0393c`；调用方
  `feature/broker-session-state` 的评审待办更新已推送至 `3c3234ca`；
- `3e21491 fix(tdxquant): handle stale signals across midnight`：完成自动确认脚本跨午夜修复；
- `22c08d0 feat(tdxquant): expose explicit session state`：完成会话状态、未登录错误契约、
  直接 HQMP 就绪探针和自动登录原型；
- 服务端全量测试结果：`328 passed`；`python -m compileall -q cn_broker_api tests` 通过；
  `git diff --check` 通过，仅有 Git 提示工作区未来可能按 Windows 配置把 LF 转为 CRLF；
- 调用方网关与客户端相关测试 `67 passed`，生产职责路径回归 `608 passed`，
  跨仓库真实 HTTP/纸面驱动契约测试 `7 passed`；仓库根执行的
  `flake8 backend --select=F --extend-ignore=F541` 通过；
- 历史抓包离线复核确认 807/822 结果字段为 `wtbh/zqdm/setcode/bsflag/wtsl/wtjg/cjsl/cjjg/cdflag/`
  `kcdflag/wtsj/ztsm`，与本轮状态机读取字段一致；
- 本轮曾按操作者授权通过 `QuantTradeDemo` CLI 解密并提交一次真实交易密码；密码未打印、未落盘。
  随后通过正式 CLI/HTTP 完成两笔真实报单与撤单，两笔最终均已撤；文档不记录完整委托编号。
- 历史实验目录中的 `hqmp-direct-13575.out.log` 含明文资金账号和资产明细，它不在仓库内，
  但仍应在保留必要的非敏感协议证据后做脱敏归档或删除。本轮未擅自改动该外部文件。

登录后的通道检查中，交易账号和自动确认时段覆盖均通过，客户端行情快照曾出现一次读取超时。该现象不能
倒推为未登录：同一轮账户与资产探针仍然成功，随后按相同参数复跑时四项全部通过；后续实盘交易前仍应
重新运行通道检查，并以首笔委托回执作为最终通道判据。

当前可以明确做到：调用方可先查询会话状态；业务请求遇到确切未登录证据时可收到稳定、机器可读的
`broker_login_required`，再调用 `/v1/session/ensure`。直接 HQMP 已完成自动登录、正式 HTTP 查询以及担保买入、
融资买入的真实报撤验收；当前还不能宣称做到的是首次冷启动注册竞态的自动恢复、断线自动重连、长期无人值守，
以及生产自动策略切换到新通道和更高交易限额。

### 下一步待办

1. **P0（已完成）：修复自动确认脚本的跨午夜时间判断**
   - 已用 `DAY + TIME` 构造完整时间戳，不再只比较当天秒数；
   - 测试台架已使用固定时钟，覆盖同日陈旧、跨午夜陈旧和未来时间三种情况；
   - 完整测试已恢复全绿。

2. **P1（核心接入与真机验收已完成，待异常场景和长时运行验收）：直接 HQMP 自动化交易程序登录**
   - 复用 `login.py` 和 `TdxQuantDriver.ensure_logged_in()` 的状态驱动流程，不另写一套只依赖弹窗消失的脚本；
   - 复用现有 `cred_source=file/request`、内存凭证库、每日提交次数限制和连续失败锁闸；
   - 将登录成功判据替换为直接 HQMP 的 `RegisterClient -> OperateUser_0 -> 资产字段校验`，不依赖 MCP；
   - 增加明确的会话状态识别，至少区分 `READY`、`LOGIN_REQUIRED`、`LOGIN_IN_PROGRESS`、
     `MANUAL_ACTION_REQUIRED`、`LOGIN_LOCKED`、`CHANNEL_UNAVAILABLE`，不得把未登录混成空账户、
     空持仓、普通查询失败或未知的 500；
   - `LOGIN_REQUIRED` 只能由确切证据判定，例如已识别到交易登录框，或 HQMP 已注册但账户/资产查询
     返回已核验的“未登录”结果。证据不足时返回 `CHANNEL_UNAVAILABLE`，不能猜成未登录；
   - 所有账户查询和交易入口执行前统一调用 `require_ready()`。确认未登录时不发送业务调用，向调用方
     返回稳定错误 `broker_login_required`、当前 `session_state` 和登录入口 `/v1/session/ensure`；
   - 经纪账户未登录建议继续使用 HTTP `503`，而不是 `401`：本项目的 `401` 已表示 API token
     鉴权失败，两种身份状态必须让调用方能够无歧义地区分；
   - 复用现有幂等、单飞的 `POST /v1/session/ensure` 作为登录入口，并使其支持直接 HQMP。调用方收到
     `broker_login_required` 后可调用该接口重新登录；已有登录任务时返回同一个任务，不并发启动客户端
     或重复提交密码；
   - 增加 `GET /v1/session/status`，供调用方在交易前主动查询状态，也供登录任务完成后确认账户和资产
     已可用。状态接口不得返回密码、完整账号、资产明细或窗口截图；
   - 先启动 HQMP 监听，再启动路径核验过的 `TC.exe`，定位并解决纯宿主冷启动时 TC 不注册或提前退出的问题；
   - 先复现并记录“TCP 已连接但未注册”时单次连接重置的实际执行结果；若重连仍不发送 `RegisterClient`，
     将已经真机验证过的“保留已登录 TC、只重启 HQMP 宿主并复用”的恢复步骤收进受控状态机；
   - 如果短期仍需原厂宿主热切换，应由一个受控编排器管理两个宿主的启动、登录验证、端口交接和重连，
     不能依赖人工停止进程或固定等待时间；
   - 登录窗口只在账号、密码框和登录按钮都唯一识别、密码星号宽度校验通过时提交一次；验证码、二次认证、
     协议确认或界面变版一律进入“需要人工处理”，不得猜测点击或循环提交；
   - 登录成功后才最小化窗口；失败时保留现场，并输出脱敏状态、失败阶段和人工处理提示；
   - 进程、端口、账户和会话必须一一对应，防止已有正式客户端运行时接管错误实例；
   - 凭证不得写日志或仓库。后续可评估 Windows Credential Manager/DPAPI，替代长期明文凭证文件；
   - 增加冷启动、已登录复用、密码错误、验证码、客户端升级、断线重登和失败次数耗尽的自动化测试。

   纯 Python 宿主冷启动、实验 `TC.exe` 注册、数据库凭证登录和 `READY` 复查都曾真机通过，无需原厂宿主热切换；
   但 2026-09-10 17:00 再次出现登录后不注册，长期无人值守仍不成立。下一轮先验收受控的宿主重启复用恢复，
   再用 `QuantTradeDemo` 的 `tdx_order.py ... --go --cancel-after` 补做一笔只传证券代码的 100 股报撤闭环；
   之后验证断线重连和持久化恢复。

   建议的登录状态机为：

   ```text
   STOPPED
     -> HOST_LISTENING
     -> TC_STARTED
     -> LOGIN_DIALOG_DETECTED
     -> CREDENTIAL_SUBMITTED_ONCE
     -> HQMP_REGISTERED
     -> ACCOUNT_AND_ASSET_VERIFIED
     -> READY

   任一步识别不唯一或需要验证码 -> MANUAL_ACTION_REQUIRED
   密码失败达到阈值             -> LOGIN_LOCKED
   DLL/界面/协议版本不匹配       -> VERSION_UNSUPPORTED
   ```

   未登录时建议的交易/查询接口响应为：

   ```json
   {
     "error": "broker_login_required",
     "message": "交易账户尚未登录",
     "session_state": "LOGIN_REQUIRED",
     "login_endpoint": "/v1/session/ensure",
     "retryable_after_login": true
   }
   ```

   调用方处理顺序为：

   ```text
   调用交易或账户查询
     -> 收到 broker_login_required
     -> POST /v1/session/ensure
     -> 200 READY，或按 202 返回的 job_id 查询任务
     -> GET /v1/session/status 确认 READY
     -> 重新发起原业务请求
   ```

   业务请求本身不隐式触发登录。这样可以避免一次普通查询突然弹窗、提交密码或启动交易程序，也能让
   调用方明确决定等待、提示人工处理还是取消本次交易。核心状态识别、HTTP 映射和单飞登录已有契约测试；
   密码错误、验证码、客户端升级、断线重登和失败次数耗尽仍需补充自动化与真机验收。

3. **P1（实现、HTTP 接入及两类真实报撤已完成）：可靠的报单—撤单状态机**
   - 已按委托编号轮询 `DoLevinGN_807/822`，并拒绝相同编号对应多条记录或两份视图证券代码冲突；
   - 已实现只有记录明确可撤时才调用 `DoLevinGN_808`，随后继续查询到已撤、已成或超时状态未定；
   - 已覆盖首次撤单“没有对应的委托信息”后重新观察再试一次、撤单回调超时只对账不重发、全部成交不撤、
     部分成交后撤单保留成交量，以及查询断线/超时不误判已撤；
   - `HqmpDirectTrading` 已完成 `TradingPort` 字段映射、驱动/HTTP 装配和离线测试，并已由
     `QuantTradeDemo` CLI 完成担保买入、融资买入的真实小额报撤验收；
   - 本地持久化恢复尚未实现；后续日志和文档仍只允许显示脱敏委托编号。

4. **P1（手工正式入口已验收，生产自动策略尚未接通）：接入正式交易接口**
   - 已为 `HqmpDirectSession` 增加符合 `TradingPort` 形状的适配器，并补齐全量委托、持仓和资产查询；
   - 证券代码是唯一委托身份；`QuantTradeDemo` 网关和手工 CLI 已移除名称查询与 `--name`，
     服务端也不接受名称作为 `TradingPort` 字段；
   - 已通过明确配置选择现有 MCP/ctypes 通道或直接 HQMP 通道，禁止静默回落；
   - 已接入 `/v1/orders`、撤单和查询端点，并保留实盘开关、单笔数量和金额上限；
   - 已区分参数拒绝、柜台拒绝、状态未知和登录失效；版本不支持的识别仍需继续补齐。
   - 尾盘调仓和日内回转原本就只传代码，现与直接 HQMP 契约一致；2026-09-10 已在
     `main` 完成不发单的生产配置启动验收；
   - 当前真机配置的 100 股/2000 元上限只适合验收，不能承载生产调仓；放宽前要增加组合级限额和回归验证。

## 正式切换记录（2026-09-10）

- `feature/tdx-headless-trade` 已快进合入 `main`，`main` 推送至提交 `7a0393c`；
- 本机忽略配置已切为 `desktop_mode=headless`、`transport=hqmp`、`hqmp_enable_trade=true`；
- `hqmp_reuse_tc=true`：没有 TC 时允许冷启动，服务重启时只接管路径精确匹配的单个实验副本 TC；
- Windows 计划任务 `cn-broker-api` 已启用并启动，17710（HTTP）与 13575（HQMP）均正常监听；
- 计划任务停止后实验副本 `TC.exe` 保持运行；重新启动任务后服务按路径接管原进程，
  `/v1/session/status` 直接恢复 `READY`，没有再次提交密码；
- 通过 QuantTradeDemo 的 `tdx_login.py --go --type CREDIT --wait 90` 完成无人干预登录；
- `tdx_channel_check.py --type CREDIT` 返回 4 项通过，并确认直接 HQMP 不依赖页面自动确认补丁；
- `tdx_order.py inspect --type CREDIT` 完成资产、持仓和当日委托只读查询：持仓 5 条、当日委托 0 条；
- 本轮正式切换未发送任何委托；生产配置继续保留单笔 100 股、2000 元的验收限额，超过限额会明确拒绝。

5. **P2：补齐协议和实盘覆盖范围**
   - 真机验证上证 `setcode=1`；
   - 覆盖普通买卖、担保卖出、融资买入、卖券还款等委托类型；
   - 覆盖部分成交后撤单、不可撤、资金不足、休市拒单、重复回调和连接中断；
   - 接入可靠行情与价格笼子校验，不再由调用脚本临时选择价格。

6. **P2：解除历史实验资源依赖**
   - 将非敏感协议模板按版本管理，运行时不再依赖历史抓包文件；
   - 为 DLL、TC 和协议版本建立兼容性清单，未核验版本保持失败关闭；
   - 保证密钥、凭证、资金账号、资产明细和完整委托编号始终不进入仓库。

7. **P3：迁移与退场方案**
   - 直接 HQMP 稳定前保留现有 MCP/页面自动确认通道作为显式可选回退；
   - 完成一段时间的并行观测和对账后，再决定是否移除页面注入与热切换实验代码。
